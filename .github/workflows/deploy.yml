name: Node.js CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      # 1️⃣ Lấy mã nguồn
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Thiết lập QEMU (hỗ trợ build đa nền tảng)
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      # 3️⃣ Thiết lập Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          install: true
          version: latest

      # 4️⃣ Đăng nhập Docker Hub
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 5️⃣ Build và push image lên Docker Hub
      - name: Build and push Docker image
        env:
          DOCKER_BUILDKIT: 1
          RAW_USERNAME: ${{ secrets.DOCKER_USERNAME }}
        run: |
          echo "🔧 Building and pushing Docker image..."

          # Xử lý lỗi xuống dòng trong secrets
          USERNAME=$(echo "$RAW_USERNAME" | tr -d '\n' | tr -d '\r')
          IMAGE_NAME="${USERNAME}/nodejs-cicd-sample:latest"

          echo "🧱 Building image: $IMAGE_NAME"

          # Tạo builder nếu chưa có
          docker buildx create --use --name mybuilder || docker buildx use mybuilder
          docker buildx inspect --bootstrap

          # Build và push image
          docker buildx build --platform linux/amd64 -t "$IMAGE_NAME" . --push



            echo "🛑 Stopping old container (if exists)..."
            docker stop nodejs-cicd-sample || true
            docker rm nodejs-cicd-sample || true

            echo "▶️ Running new container..."
            docker run -d -p 3000:3000 --name nodejs-cicd-sample ${{ secrets.DOCKER_USERNAME }}/nodejs-cicd-sample:latest

            echo "🧹 Cleaning up old images..."
            docker image prune -f

      # 7️⃣ Dọn dẹp sau khi build
      - name: Cleanup
        if: always()
        run: |
          docker logout
